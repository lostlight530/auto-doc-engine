# March 2025 Reconstruction — Stage E

March closes the quarter with two distinct lifecycle observations.

Pandoc 3.6.4 (2025-03-16) changes citation handling when `--citeproc` is used and fixes reader behavior. PEP 751 reaches `Final` on 2025-03-31, turning the previously proposed Python lock-file effort into a normative `pylock.toml` standard.

The month therefore connects converter drift with environment-description standardization:

```text
converter revision identity
+
standardized dependency-lock identity
!= reproduced execution environment
```

RO-Crate 1.2 remains outside the Q1 release window; its later official release is used only as a temporal boundary.
