---
title: "{{ project.name }} — Project Status"
description: "Generated project-status snapshot"
status: "{{ document_status|default('active', true) }}"
updated: "{{ generated_at|default('2026-08-23', true) }}"
{% if artifact_id is defined %}artifact_id: "{{ artifact_id }}"
{% endif %}{% if source_refs is defined %}sources: {{ source_refs|tojson }}
{% endif %}---

# {{ project.name }} — Project Status

**Current status:** {{ project.status }}

## Milestones

{{ milestones|table(['里程碑', '截止日期', '状态', '进度']) }}

## Blockers

{% if blockers %}
{{ blockers|bullet_list }}
{% else %}
No blockers declared.
{% endif %}

## Health signals

{% if health_metrics %}
{% for metric in health_metrics %}
- {{ metric.name }}: {{ metric.value }}{% if metric.trend is defined %} ({{ metric.trend }}){% endif %}
{% endfor %}
{% else %}
No health signals declared.
{% endif %}

## Evidence boundary

This report is a structured project snapshot. Status values and health signals reflect supplied data; they are not independently verified by document generation alone.
